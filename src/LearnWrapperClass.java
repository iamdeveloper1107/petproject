/**
 * Wrapper Class = provides a way to use primitive data types as reference data types
 *                  references data type contain useful method
 *                  can be used with collections (ex.ArrayList)
 * 
 * primitive:   boolean, char, int, double
 * wrapper:     Boolean, Charaterm, Integer, Double
 * 
 * autoboxing = the automatic conversion that the Java compiles make between the primitive type and their corresponding wrapper class
 * unboxing = the reverse of autoboxing. Automatic conversion of wrapper class to primitive type
 */
public class LearnWrapperClass {
    
    public static void main(String[] args) {
        Boolean a = true;
        Character b = '@';
        Integer c = 123;
        Double d = 3.14;
        String e  = "Say";

        
    }
}
